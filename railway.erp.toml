[build]
builder = "NIXPACKS"
buildCommand = "npm install --include=dev && npm run build --workspace=@sukuu/erp"

[deploy]
startCommand = "cd apps/erp && npx next start -p $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
